# Core dependencies for tiddl
typer>=0.9.0
requests>=2.31.0
requests-cache>=1.2.0
rich>=13.7.0
mutagen>=1.47.0
aiohttp>=3.9.0
aiofiles>=23.2.0
tqdm>=4.66.0
pycryptodome>=3.20.0
ffmpeg-python>=0.2.0
python-dotenv>=1.0.0
# CRITICAL: Pydantic v1 is required for compatibility with current codebase
# (uses .parse_obj() which was renamed in v2). Do NOT upgrade to v2.x
pydantic<2.0
pymp4>=1.4.0
m3u8>=3.6.0
# Compatibility for Python 3.10 (3.11+ has tomllib built-in)
tomli>=2.0.0;python_version<"3.11"
# Optional: for better transliteration support
unidecode>=1.3.0

# Optional: for building executables
# pyinstaller>=6.3.0

# Optional: for development
# pytest>=7.0
# mypy>=1.0
# black>=23.0
