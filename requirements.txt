# Runtime dependencies are declared in pyproject.toml — the SINGLE source of
# truth. This file used to duplicate that list and drifted (it was missing
# `click` and `filelock`, both required, so `pip install -r requirements.txt`
# produced a broken install). It now just installs the project itself, so the
# dependency set can never diverge from pyproject.toml again.
#
#   pip install -r requirements.txt      # installs tiddl + runtime deps (editable)
#   pip install -e ".[dev]"              # + test/lint tooling (pytest, ruff, mypy)
#
# For a reproducible, pinned environment see requirements.lock (generated from
# pyproject.toml with `uv pip compile`).
-e .
